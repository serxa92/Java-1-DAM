Ejercicio 3:
🎯 Objetivo:
Practicar herencia con constructores, uso de super() y reutilización de métodos del padre.

📋 Enunciado:
Crea una clase Vehiculo con:

Atributos: marca (String), año (int)

Constructor para inicializarlos

Método mostrarDatos() que imprima esos datos

Crea una clase Coche que herede de Vehiculo y añada:

Atributo: puertas (int)

Constructor que use super() para los atributos heredados

Método mostrarCoche() que imprima toda la información

Crea una clase Moto que herede de Vehiculo y añada:

Atributo: tieneCasco (boolean)

Constructor con super()

Método mostrarMoto() que imprima los datos

En la clase Main, crea un objeto Coche y otro Moto, y llama a sus métodos para mostrar la información por consola.


Herencia con Constructores y uso de super()
